{
  "name": "darkapps/render",
  "description": "File sharing with ads",
  "type": "project",
  "license": "MIT",
  "require": {
    "php": ">=8.1"
  },
  "scripts": {
    "post-install-cmd": [
      "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
    ]
  }
}
